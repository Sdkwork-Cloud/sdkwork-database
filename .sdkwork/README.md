# sdkwork-database Local Workspace

This directory contains local workspace metadata for the sdkwork-database project.

## Structure

- `skills/`: local agent skills
- `plugins/`: local agent plugins
- `README.md`: this file

## Purpose

sdkwork-database is the unified database connection pool management library for all SDKWork applications.
